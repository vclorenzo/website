import styled from 'styled-components';

export const VideoBg = styled.video`
  height: 100%;
  width: 100%;
  object-fit: cover;
  -o-object-fit: fill;
  position: absolute;
  z-index: -1;
  top: 0;
`;
